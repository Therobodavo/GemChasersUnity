using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class IType
{
    public enum ElementType { NoType,Heat, Breeze, Forest, Music, Space, Water };
    public enum BuffType {Strength,Speed,Shield,Heal,Relax,Split,Combo};
    public enum GemType { Heat, Breeze, Forest, Music, Space, Water };
}
